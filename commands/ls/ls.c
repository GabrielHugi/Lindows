#include <windows.h>
#include <stdio.h>
#include <stdlib.h>

int main (int argc, char** argv) {
    setlocale(LC_ALL, "");
    WIN32_FIND_DATA findData;
    HANDLE fileHandler = NULL;

    char ath[2048]
}